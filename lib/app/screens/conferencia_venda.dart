import 'package:flutter/material.dart';

class ConferenciaVenda extends StatelessWidget {
  const ConferenciaVenda({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
